services:
  notes:
    build: .
    ports:
      - "3001:3001"
    volumes:
      - ./data:/data
    environment:
      JWT_SECRET: ${JWT_SECRET:-change-me}
      NODE_ENV: production
      NOTES_DB_PATH: /data/notes.db
    restart: unless-stopped
